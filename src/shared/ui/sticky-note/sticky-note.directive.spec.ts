import { StickyNoteDirective } from './sticky-note.directive';

describe('StickyNoteDirective', () => {
  it('should create an instance', () => {
    const directive = new StickyNoteDirective();
    expect(directive).toBeTruthy();
  });
});
