import { SideNavTriggerDirective } from './side-nav-trigger.directive';

describe('SideNavTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new SideNavTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
